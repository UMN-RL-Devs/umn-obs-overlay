/**
 * TODO: This component should be the player stat card in the bottom left corner of the overlay.
 * It should only be displayed when spectating a player.
 */
export const PlayerStatCard = () => {
  return <></>;
};
